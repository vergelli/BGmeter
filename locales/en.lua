-- bgmeter :: locales/en.lua
-- English overrides. Loaded after default.lua. Nothing to override yet --
-- default.lua already holds the English strings; this file exists so the
-- manifest's locales/$(language).lua entry resolves on EN clients.
BGMeter = BGMeter or {}
